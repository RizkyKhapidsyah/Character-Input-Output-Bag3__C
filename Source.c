#include <stdio.h>
#include <conio.h>

int main()
{
	int a,b,c;

	printf("Type three letters: ");
	a = getchar();
	putchar(a);
	b = getchar();
	putchar(b);
	c = getchar();
	putchar(c);

	_getch();
	return(0);
}

